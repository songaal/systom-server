<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notification">

    <select id="select" parameterType="UserNotification" resultType="UserNotification">
        select user_id
        , title
        , service_name
        , service_user
        , create_time
        from user_notification
        <if test="userId != null and userId != ''">
        where user_id = #{userId}
        </if>
        <if test="serviceName != null and serviceName != ''">
            and service_name = #{serviceName}
        </if>
    </select>


    <insert id="insert" parameterType="UserNotification">
        INSERT INTO user_notification
        (user_id, title, service_name, service_user)
        VALUES
        (#{userId},
        #{title},
        #{serviceName},
        #{serviceUser}
        )
    </insert>

    <delete id="delete" parameterType="UserNotification">
        DELETE FROM user_notification
        WHERE user_id = #{userId}
        and service_name = #{serviceName}
    </delete>
</mapper>
