<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userCoin">

    <insert id="insertUserCoin" parameterType="String">
        INSERT INTO user_coin (user_id, cash)
        VALUES ( #{param1}
               , 0
        )
    </insert>

    <update id="updateAmount" parameterType="User">
        UPDATE user_coin
           SET cash = cash + ${cash}
         WHERE user_id = #{userId}
    </update>

    <select id="getUserCoin" parameterType="String" resultType="User">
        SELECT user_id
             , cash
          FROM user_coin
         WHERE user_id = #{userId}
    </select>

</mapper>
