<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="testing">

    <insert id="insertTestHistory" parameterType="Task">
        INSERT INTO test_history
        (strategy_id,
        strategy_version,
        test_time,
        exchange_name,
        coin,
        start_time,
        end_time,
        user_id,
        ecs_task_id)
        VALUES
        ( #{strategyId}
        , #{strategyVersion}
        , now()
        , #{exchangeName,jdbcType=VARCHAR}
        , #{coin}
        , #{startTime}
        , #{endTime}
        , #{userId}
        , #{ecsTaskId,jdbcType=VARCHAR}
        )
    </insert>

    <select id="selectAllTestHistory" parameterType="Task" resultType="Task">
        SELECT id,
            strategy_id,
            strategy_version,
            action,
            test_time,
            exchange_name,
            coin,
            revenue,
            start_time,
            end_time,
            user_id,
            ecs_task_id
        FROM test_history
        WHERE 1=1
        AND USER_ID = #{userId}
        AND strategy_id = #{strategyId}
        ORDER BY test_time desc
        LIMIT 1
    </select>


</mapper>
