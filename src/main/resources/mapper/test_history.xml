<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">

    <insert id="insertTestHistory" parameterType="Task">
        INSERT INTO test_history
        (strategy_id,
        strategy_version,
        test_time,
        exchange_name,
        coin,
        start_time,
        end_time,
        user_id,
        ecs_task)
        VALUES
        ( #{strategyId}
        , #{strategyVersion}
        , now()
        , #{exchangeName,jdbcType=VARCHAR}
        , #{coin}
        , #{startTime}
        , #{endTime}
        , #{userId}
        , #{ecsTask,jdbcType=VARCHAR}
        )
    </insert>

    <select id="lastBackTest" parameterType="Task" resultType="Task">
        SELECT id,
            strategy_id,
            strategy_version,
            action,
            test_time,
            exchange_name,
            coin,
            revenue,
            start_time,
            end_time,
            user_id,
            ecs_task
        FROM test_history
        WHERE 1=1
        AND USER_ID = #{userId}
        AND strategy_id = #{strategyId}
        ORDER BY test_time desc
        LIMIT 1
    </select>

    <select id="getBackTestHistory" parameterType="String" resultType="Task">
        SELECT a.id,
            a.strategy_id,
            a.strategy_version,
            a.action,
            a.test_time,
            a.exchange_name,
            a.coin,
            a.revenue,
            a.start_time,
            a.end_time,
            a.user_id,
            b.version
        FROM test_history a
        JOIN strategy b on a.strategy_id = b.id
        WHERE 1=1
        AND a.strategy_id = #{strategysId}
        ORDER BY a.test_time desc
        LIMIT 10
    </select>

</mapper>
